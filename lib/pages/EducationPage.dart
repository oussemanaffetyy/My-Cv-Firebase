import 'package:flutter/material.dart';

class EducationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EducationPage'),
      ),
      body: Center(
        child: Text('Bienvenue sur l\'écran d\'Education !'),
      ),
    );
  }
}