import 'package:flutter/material.dart';

class skill extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('skill'),
      ),
      body: Center(
        child: Text('Bienvenue sur l\'écran d skill !'),
      ),
    );
  }
}